# Pinned deliberately.
#
# corpus/INDEX.md pins the DATA with a sha256. This file pins the CODE that turns
# that data into numbers. Without it, a minor version bump in sentence-transformers
# can change embedding values, which changes every metric in results/ — with no
# hash to catch it. That is Day 1's corpus-drift problem one layer up.
#
# Python 3.13 (not 3.14): torch 2.13 has wheels for 3.13 today.
# Recreate with:  python3.13 -m venv .venv && .venv/bin/pip install -r requirements.txt

sentence-transformers==5.6.1
torch==2.13.0
transformers==5.14.1
tokenizers==0.22.2
numpy==2.5.1
pytest==9.1.1
